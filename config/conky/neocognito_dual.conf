conky.config = {
    -- Window Settings
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 50,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    
    -- Position
    xinerama_head = 1,
    alignment = 'top_right',
    gap_x = 30,
    gap_y = 60,
    minimum_width = 400,
    maximum_width = 400,
    
    -- Graphics & Text
    double_buffer = true,
    use_xft = true,
    font = 'Monospace:size=11',
    xftalpha = 1,
    
    -- Colors
    default_color = '#FFFFFF',
    color1 = '#00FF99', -- Accent
    color2 = '#FFCC00', -- Warning
    
    -- Refresh
    update_interval = 30,
};

conky.text = [[
${color1}${font Monospace:style=Bold:size=16}INBOX (Capture)${font}${color}
${hr 2}
${execp ~/Dev_Pro/NeoCognito/scripts/render_bold.sh ~/Vault/00_Inbox.md tail}

${voffset 30}${color1}${font Monospace:style=Bold:size=16}TODO TODAY (Focus)${font}${color}
${hr 2}
${execp ~/Dev_Pro/NeoCognito/scripts/render_bold.sh ~/Vault/10_Planning/TODO_Today.md cat}

${voffset 30}${font Monospace:size=9}${color grey}Updated: ${time %H:%M} | Auto-save: On${color}${font}
]];
